package pro.branium.messenger.domain.model.enums

/**
 * Defines possible gender identities. Adapt as needed for inclusivity.
 */

enum class Gender {
    MALE,
    FEMALE,
    NON_BINARY,
    OTHER,
    PREFER_NOT_TO_SAY
}